@model AccountManager.Models.FinancialYear

@{
    ViewBag.Title = "Edit";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}

<script src="~/Content/ckeditor/ckeditor.js"></script>

@Html.StarkAjaxFormSubmiter("frmFinancialYear", "tbFinancialYear", true, "Action Successfully Executed")

 
@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
            <h1>
                @ViewBag.Title
                <small>@ViewBag.Title</small>
            </h1>
        </section>
    }
}

<script type="text/javascript">
        $(function () { 
            $('select').searchableOptionList({
                maxHeight: '250px'
            });
        });
</script>

@using (Html.BeginForm("Edit", "FinancialYear", FormMethod.Post, new { enctype = "multipart/form-data",id = "frmFinancialYear"  }))
{
    @Html.AntiForgeryToken()

        <!-- Main content -->
    <section class="content">
        <!-- Small boxes (Stat box) -->
        <div class="row">
            <div class="col-xs-12">
                <div class="box box-solid box-primary">
                    <div class="box-header">
                        <h3 class="box-title">Edit FinancialYear</h3>
                    </div> 
                    <!-- form start -->
  
                    <div role="form">
                        <div class="box-body">
                              

                            <div class="form-group">
                                @Html.LabelFor(model => model.StartDate, new { @class = "" }) 
                                @Html.EditorFor(model => model.StartDate, new { htmlAttributes = new { @class = "" } })
                                <script> $(function () { $("#StartDate").datepicker(); }); </script> 
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.EndDate, new { @class = "" }) 
                                @Html.EditorFor(model => model.EndDate, new { htmlAttributes = new { @class = "" } })
                                <script> $(function () { $("#EndDate").datepicker(); }); </script> 
                            </div>


                        </div><!-- /.box-body -->
                        <div class="box-footer">
                            @Html.StarkInputButtion("Save","submit","btn btn-primary","") 
                            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

}


