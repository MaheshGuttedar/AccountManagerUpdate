@model AccountManager.Models.Transaction

@{
    ViewBag.Title = "Are you sure you want to delete";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}


@Html.StarkAjaxFormSubmiter("frmTransaction", "tbTransaction", true, "Action Successfully Executed")

@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
            <h1>
                @ViewBag.Title
                @*<small>@ViewBag.Title</small>*@
            </h1>
        </section>
    }
}

<section class="content">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-xs-12">

            <div class="box box-solid box-primary">
                <div class="box-header">
                    <h3 class="box-title">Detail of Transaction</h3>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">
                  
                    <div class="widget-sec">
                        <div class="streaming-table">
                            <table class="table table-striped table-bordered">

                                <tr>
                                    <td>Title</td>
                                    <td>@Html.DisplayFor(model => model.Title)</td>
                                </tr>
                                <tr>
                                    <td>DateAdded</td>
                                    <td>@Html.DisplayFor(model => model.DateAdded)</td>
                                </tr>
                                <tr>
                                    <td>AddedBy</td>
                                    <td>@Html.DisplayFor(model => model.AddedBy)</td>
                                </tr>
                                <tr>
                                    <td>OfficeId</td>
                                    <td>@Html.DisplayFor(model => model.OfficeId)</td>
                                </tr>
                                <tr>
                                    <td>DebitAccount</td>
                                    <td>@Html.DisplayFor(model => model.DebitAccount)</td>
                                </tr>
                                <tr>
                                    <td>DebitAmount</td>
                                    <td>@Html.DisplayFor(model => model.DebitAmount)</td>
                                </tr>
                                <tr>
                                    <td>CreditAccount</td>
                                    <td>@Html.DisplayFor(model => model.CreditAccount)</td>
                                </tr>
                                <tr>
                                    <td>CreditAmount</td>
                                    <td>@Html.DisplayFor(model => model.CreditAmount)</td>
                                </tr>
                                <tr>
                                    <td>TransactionDate</td>
                                    <td>@Html.DisplayFor(model => model.TransactionDate)</td>
                                </tr>


                            </table>
                            <div class="box-footer">

                                  @using (Html.BeginForm("Delete", "Transaction", FormMethod.Post, new {  id = "frmTransaction" }))
                                {
                                    @Html.AntiForgeryToken()

                                    @Html.StarkInputButtion("Delete","submit","btn btn-danger","")
                                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



